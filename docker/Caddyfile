{
    admin off
    auto_https off
}

0.0.0.0:{$PORT:8080} {
    root * /app/public
    encode zstd br gzip

    @versioned_assets path /build/assets/*
    header @versioned_assets Cache-Control "public, max-age=31536000, immutable"

    php_server {
        try_files {path} index.php
    }
}
